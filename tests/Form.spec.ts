// import { render, screen, fireEvent, waitFor } from '@testing-library/react';
// import { Form } from "@/components";

describe("Form component", () => {
  describe("given 'deposit' action", () => {
    it("switch action when the deposit toggle button is clicked", () => {});

    it("set the maximum allowed for 'deposit'", () => {});

    it("set error state when invalid input is entered", () => {});

    it("set error state when exceeding allowance for 'deposit'", () => {});

    it("set error state when exceeding wallet balance for 'deposit'", () => {});

    it('call Bank.Movement with correct props when "deposit" action is selected', () => {});
  });

  // TODO: Similar tests for the other actions
});
